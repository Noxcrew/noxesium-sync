Noxesium Sync
---
An extension to Noxesium to let you synchronize folders in the server directory with clients. This can be used to simplify development by modifying configurations from your client instead of having to use an FTP connection. It also speeds up testing significantly if servers are ran inside Docker containers as it allows modifying configs of running servers. This module should **never** be enabled on production servers.
